{
  "name": "vent.monorepo",
  "description": "Monorepo root of the vent app template.",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prettier": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^18.11.2",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  }
}
