{
  "name": "vent.client",
  "description": "Electron window management and preload scripts.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test.ui": "vitest --ui",
    "test": "vitest",
    "build": "npm run compile-rollup && rollup --config rollup.config.js",
    "compile-rollup": "tsc -p tsconfig.rollup.json"
  },
  "devDependencies": {
    "electron": "^21.1.1"
  }
}
